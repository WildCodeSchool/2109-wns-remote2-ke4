version: "3.1"

services:
  server:
    build: ./back
    restart: always
    ports:
      - 4000:4000
    volumes:
     - ./back/src:/back/src  
  
  client:
    build: ./front
    ports:
      - 3000:3000
    volumes:
     - ./front/src:/app/src
   
  postgres:
    image: postgres
    container_name: postgres
    volumes: 
      - ./data:/var/lib/postgresql/data
    restart: always
    ports:
      - 5432:5432
    
    environment:
      POSTGRES_PASSWORD: ke4db
      POSTGRES_USER: ke4user
      POSTGRES_DB: ke4
    
      